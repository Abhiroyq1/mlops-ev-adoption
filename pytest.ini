[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*

markers =
    integration: marks tests as integration tests that require the real dataset (deselect with -m "not integration")

addopts =
    --tb=short
    -q
